# قائمة المهام (TODO)

**المشروع:** منصة مكاتب المحاماة
**المطور:** يونس ضاعني
**تاريخ آخر تحديث:** 2025-10-02

## ما تم إنجازه:

### 1. إعادة الهيكلة والتنظيم:
*   تم إنشاء هيكل مجلدات احترافي وواضح (MVC Pattern).
*   تم نقل جميع الملفات الأساسية إلى مواقعها الجديدة.
*   تم تحديث ملفات `config.php` و `database.php` و `app.php`.
*   تم إنشاء ملف `functions.php` للدوال المساعدة.
*   تم إنشاء ملفات Core Framework (`Database.php`, `Router.php`, `Controller.php`, `Auth.php`).
*   تم إنشاء ملف `.htaccess` في المجلد `public` لإعادة توجيه الطلبات.
*   تم تحديث `public/index.php` ليكون نقطة الدخول الرئيسية للتطبيق باستخدام نظام التوجيه الجديد.

### 2. إكمال المشروع - قاعدة البيانات:
*   تم إنشاء ملف `database/schema.sql` لتحديد هيكل قاعدة البيانات.
*   تم إنشاء قاعدة البيانات والجداول بنجاح.
*   تم تحديث `app/config/database.php` ببيانات الاتصال الصحيحة.

### 3. إكمال المشروع - الواجهة الأمامية (Frontend):
*   تم إعادة تصميم الواجهة الرئيسية (`home.php`) لتكون حديثة وجذابة باستخدام Ionic Framework.
*   تم تصميم صفحات تسجيل الدخول (`login.php`) والتسجيل (`register.php`) بتصميم App-like.
*   تم تحديث ملفات CSS (`style.css`, `mobile-ionic.css`) لتناسب التصميم الجديد.
*   تم تحديث ملف JavaScript (`main.js`) لإضافة تفاعلات وتحسينات للواجهة.
*   تم إنشاء ملف `app/views/partials/footer.php` لتضمين شريط التنقل السفلي والتذييل.
*   تم إنشاء ملف `app/views/client/dashboard.php` للوحة تحكم العميل.
*   تم إنشاء ملف `app/views/client/new_case.php` لإنشاء قضية جديدة.

### 4. إكمال المشروع - الوظائف الخلفية (Backend) والـ APIs:
*   تم إنشاء وحدات Model للتعامل مع البيانات (`User.php`, `CaseModel.php`, `Message.php`, `Consultation.php`).
*   تم إنشاء وحدات Controller للتعامل مع منطق التطبيق لكل من (`HomeController.php`, `AuthController.php`, `ClientController.php`, `LawyerController.php`, `AdminController.php`).
*   تم تعريف المسارات (Routes) الأساسية في `public/index.php` لكل من المستخدمين (العميل، المحامي، المدير).
*   تم تنفيذ وظائف تسجيل الدخول والتسجيل وإدارة الجلسات.
*   تم تنفيذ وظائف لوحة تحكم العميل، بما في ذلك عرض القضايا والاستشارات وإنشاء قضايا جديدة.
*   تم تنفيذ وظائف لوحة تحكم المحامي، بما في ذلك عرض القضايا والاستشارات والتقارير.
*   تم تنفيذ وظائف لوحة تحكم المدير، بما في ذلك عرض الإحصائيات وإدارة المحامين والقضايا.

## ما تبقى إنجازه:

### 1. إكمال الوظائف المتقدمة والتحسينات:
*   **نظام الرسائل:** إكمال واجهة المستخدم لنظام الرسائل بين المحامين والعملاء، بما في ذلك إرسال المرفقات.
*   **إدارة الملفات:** إضافة وظيفة تحميل وعرض الملفات والمستندات المتعلقة بالقضايا.
*   **إدارة الاستشارات:** إكمال واجهة المستخدم لإرسال الردود على الاستشارات من قبل المحامين.
*   **إدارة الملف الشخصي:** السماح للمستخدمين بتعديل ملفاتهم الشخصية (الاسم، البريد الإلكتروني، كلمة المرور).
*   **البحث والتصفية:** إضافة وظائف بحث وتصفية متقدمة للقضايا والاستشارات والمحامين.
*   **الإشعارات:** تطوير نظام إشعارات في الوقت الفعلي (Real-time notifications) للمستخدمين.
*   **التحقق من المدخلات:** تعزيز التحقق من صحة المدخلات (Validation) في جميع النماذج.
*   **التعامل مع الأخطاء:** تحسين صفحات عرض الأخطاء (مثل 404, 500) وتجربة المستخدم عند حدوثها.

### 2. الجودة والتنظيم:
*   **تحسين الأداء:** مراجعة الكود لتحسين الأداء وسرعة التحميل بشكل أكبر.
*   **الأمان:** مراجعة شاملة للإجراءات الأمنية (مثل حماية CSRF، منع هجمات XSS و SQL Injection).
*   **التعليقات والتوثيق:** إضافة المزيد من التعليقات التوضيحية والتوثيق للكود.
*   **اختبار الوحدة والتكامل:** كتابة اختبارات الوحدة (Unit Tests) واختبارات التكامل (Integration Tests) للوظائف الرئيسية.

## التقدم الحالي في المشروع:

*   **الهيكل الأساسي:** مكتمل بنسبة 100%.
*   **قاعدة البيانات:** مكتملة بنسبة 100% (الهيكل الأولي).
*   **الواجهة الأمامية الأساسية:** مكتملة بنسبة 90% (الصفحات الرئيسية، تسجيل الدخول/التسجيل، لوحات التحكم، إنشاء قضية جديدة).
*   **الوظائف الخلفية الأساسية:** مكتملة بنسبة 80% (إدارة المستخدمين، القضايا، الاستشارات).
*   **الوظائف المتقدمة:** مكتملة بنسبة 20%.
*   **الجودة والأداء:** قيد التحسين المستمر.

---
