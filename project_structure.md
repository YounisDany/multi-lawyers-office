# هيكل المشروع المقترح (Proposed Project Structure)

لتحسين تنظيم المشروع وسهولة صيانته وتطويره، سيتم إعادة هيكلة المشروع وفقًا للتالي:

```
multi-lawyers-office/
├── public/                  # الملفات العامة التي يمكن الوصول إليها مباشرة عبر الويب
│   ├── index.php            # نقطة الدخول الرئيسية للتطبيق (Bootstrap)
│   ├── assets/              # الأصول الثابتة (CSS, JS, صور)
│   │   ├── css/             # ملفات CSS (مثل style.css، mobile-ionic.css)
│   │   ├── js/              # ملفات JavaScript (مثل main.js)
│   │   └── images/          # صور المشروع
│   └── .htaccess            # لإعادة توجيه الطلبات (Friendly URLs)
├── app/                     # منطق التطبيق الأساسي
│   ├── config/              # ملفات الإعدادات
│   │   └── database.php     # إعدادات قاعدة البيانات
│   │   └── app.php          # إعدادات التطبيق العامة
│   ├── controllers/         # وحدات التحكم (Controllers) التي تعالج الطلبات
│   │   ├── AuthController.php
│   │   ├── ClientController.php
│   │   ├── LawyerController.php
│   │   └── AdminController.php
│   ├── models/              # نماذج البيانات (Models) التي تتفاعل مع قاعدة البيانات
│   │   ├── User.php
│   │   ├── Case.php
│   │   ├── Message.php
│   │   └── Consultation.php
│   ├── views/               # ملفات العرض (Views) التي تحتوي على HTML
│   │   ├── auth/            # عروض خاصة بالمصادقة (تسجيل الدخول، التسجيل)
│   │   │   ├── login.php
│   │   │   └── register.php
│   │   ├── client/          # عروض خاصة بالعملاء
│   │   ├── lawyer/          # عروض خاصة بالمحامين
│   │   ├── admin/           # عروض خاصة بالمسؤولين
│   │   ├── partials/        # أجزاء مشتركة من الواجهة (مثل الرأس، التذييل، شريط التنقل)
│   │   │   ├── header.php
│   │   │   ├── footer.php
│   │   │   └── navbar.php
│   │   └── home.php         # الصفحة الرئيسية
│   ├── core/                # الفئات الأساسية والمساعدات (Helpers)
│   │   ├── Database.php     # فئة الاتصال بقاعدة البيانات
│   │   ├── Router.php       # نظام التوجيه (Routing)
│   │   └── Session.php      # إدارة الجلسات
│   │   └── Auth.php         # وظائف المصادقة والتحقق من الأدوار
│   └── functions.php        # دوال مساعدة عامة (مثل redirect, hashPassword)
├── vendor/                  # مكتبات PHP الخارجية (إذا تم استخدام Composer)
├── database/                # ملفات قاعدة البيانات
│   └── schema.sql           # هيكل قاعدة البيانات (CREATE TABLE)
│   └── seeds.sql            # بيانات أولية (اختياري)
├── TODO.md                  # قائمة المهام للمشروع
├── README.md                # وصف المشروع
└── .env                     # متغيرات البيئة (لإعدادات حساسة مثل مفاتيح API)
```

## مبررات الهيكلة الجديدة:

1.  **فصل الاهتمامات (Separation of Concerns):** يتم فصل منطق العرض (Views) عن منطق التطبيق (Controllers, Models) عن الأصول الثابتة (public/assets)، مما يجعل الكود أكثر تنظيمًا وسهولة في الفهم والصيانة.
2.  **أمان أفضل:** نقل ملفات PHP الحساسة خارج مجلد `public` يمنع الوصول المباشر إليها عبر الويب، مما يعزز الأمان.
3.  **قابلية التوسع (Scalability):** الهيكل الجديد يسهل إضافة ميزات جديدة ووحدات تحكم ونماذج دون التأثير على الأجزاء الأخرى من التطبيق.
4.  **سهولة الاختبار:** فصل المكونات يجعل من السهل كتابة اختبارات الوحدة لكل جزء من التطبيق.
5.  **توافق مع المعايير:** هذا الهيكل يتبع أنماط تصميم شائعة في تطوير الويب (مثل MVC) ويجعل المشروع مألوفًا للمطورين الآخرين.
6.  **إدارة التبعيات:** إضافة مجلد `vendor` يمهد الطريق لاستخدام Composer لإدارة تبعيات PHP، مما يبسط عملية دمج المكتبات الخارجية.

## إعداد البيئة:

-   **الخادم:** سيتم استخدام خادم ويب (مثل Apache أو Nginx) مع PHP.
-   **قاعدة البيانات:** MySQL/MariaDB.
-   **التبعيات:** سيتم تضمين المكتبات الأمامية (مثل Bootstrap, Ionic) عبر CDN أو تنزيلها ووضعها في `public/assets`.
-   **Composer:** سيتم استخدامه لإدارة تبعيات PHP إذا لزم الأمر (مثل مكتبة للتعامل مع التوجيه أو القوالب).

هذا الهيكل سيوفر أساسًا قويًا لإعادة بناء المشروع وتحسينه بشكل شامل.
